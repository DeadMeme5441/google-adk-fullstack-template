# ==============================================================================
# Google ADK Template - Production Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# Container Configuration
# ------------------------------------------------------------------------------
BACKEND_IMAGE=google-adk-backend:latest
BACKEND_REPLICAS=2
HOST_PORT=8000

# ------------------------------------------------------------------------------
# Agent Configuration
# ------------------------------------------------------------------------------
AGENT_NAME=production_assistant
AGENT_MODEL=gemini-2.0-flash-exp
AGENT_DESCRIPTION=Production AI assistant powered by Google ADK

# ------------------------------------------------------------------------------
# Model Provider Configuration
# ------------------------------------------------------------------------------
MODEL_PROVIDER=gemini

# ------------------------------------------------------------------------------
# Google Cloud Configuration (Production)
# ------------------------------------------------------------------------------
GOOGLE_GENAI_USE_VERTEXAI=True
GOOGLE_CLOUD_PROJECT=your-production-project-id
GOOGLE_CLOUD_LOCATION=us-central1
# Use service account key or ADC for authentication

# ------------------------------------------------------------------------------
# LiteLLM Configuration (if needed)
# ------------------------------------------------------------------------------
# OPENAI_API_KEY=your_production_openai_key
# ANTHROPIC_API_KEY=your_production_anthropic_key
# COHERE_API_KEY=your_production_cohere_key

# ------------------------------------------------------------------------------
# Database Configuration (Production)
# ------------------------------------------------------------------------------
MONGO_URL=mongodb://username:password@mongodb.example.com:27017/database?authSource=admin
MONGO_DB_NAME=production_adk_sessions
MONGO_COLLECTION_NAME=sessions

# ------------------------------------------------------------------------------
# S3 Configuration (Production)
# ------------------------------------------------------------------------------
S3_BUCKET_NAME=your-production-artifacts-bucket
# For AWS S3:
# S3_ENDPOINT_URL=  # Leave empty for AWS S3
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# For other S3-compatible services:
# S3_ENDPOINT_URL=https://your-s3-endpoint.com

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password

# ==============================================================================
# Production Deployment Instructions
# ==============================================================================
#
# 1. Build and tag your images:
#    docker build -t google-adk-backend:latest ./backend
#    docker build -t google-adk-frontend:latest ./frontend
#
# 2. Push to your container registry:
#    docker tag google-adk-backend:latest your-registry/google-adk-backend:latest
#    docker push your-registry/google-adk-backend:latest
#
# 3. Configure environment:
#    cp deployment/.env.prod.example .env.prod
#    # Edit .env.prod with your production values
#
# 4. Deploy:
#    docker-compose -f deployment/docker-compose.prod.yaml --env-file .env.prod up -d
#
# 5. Scale if needed:
#    docker-compose -f deployment/docker-compose.prod.yaml --env-file .env.prod up -d --scale backend=4
#
# ==============================================================================